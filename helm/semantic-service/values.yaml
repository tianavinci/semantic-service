replicaCount: 2

image:
  repository: semantic-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

resources:
  requests:
    cpu: "250m"
    memory: "256Mi"
  limits:
    cpu: "1"
    memory: "512Mi"

env: {}
#  DATABASE_URL: postgresql+asyncpg://lextr_user:Mygooru1028%24@db:5433/lextr
#  REDIS_URL: redis://redis:6379/0
#  SEMANTIC_VERSION: v0.1.0
#  ENABLE_CACHE: "true"
# Dask config (example):
#  DASK_MODE: "local"      # or "remote"
#  DASK_SCHEDULER_ADDRESS: "tcp://dask-scheduler:8786"
#  DASK_LOCAL_THREADS: "0"  # 0 => auto

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
