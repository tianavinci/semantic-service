apiVersion: v1
kind: Service
metadata:
  name: {{ include "semantic-service.fullname" . }}
  labels: {{- include "semantic-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector: {{- include "semantic-service.selectorLabels" . | nindent 4 }}
